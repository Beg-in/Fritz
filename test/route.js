'use strict';

module.exports = function(assert) {
    describe('route', function() {
        it('should add tests!', function() {
            //assert.fail(undefined, undefined, 'add tests!');
        });
    });
};
